/**
 * @file md_parser.h
 * @brief Модуль для парсинга Markdown-файлов и построения таблиц частот слов для цепей Маркова.
 * 
 * Обрабатывает все .md файлы из заданной папки, выделяет слова (игнорируя пунктуацию и разметку),
 * строит для каждого файла таблицу переходов между соседними словами и сохраняет результат
 * в текстовый файл с расширением .txt в указанную выходную папку.
 */

#ifndef MD_PARSER_H
#define MD_PARSER_H

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Обрабатывает все Markdown-файлы из входной директории и сохраняет таблицы частот.
 * 
 * Для каждого файла с расширением .md строится таблица частот переходов между словами (первый порядок).
 * Результат сохраняется в файл с тем же именем, но с расширением .txt, в выходную папку.
 * Если выходная папка не существует, она будет создана.
 * 
 * @param input_dir  путь к папке с входными .md файлами (например, "input")
 * @param output_dir путь к папке для выходных .txt файлов (например, "output")
 * 
 * @note Функция завершает программу через exit() при фатальных ошибках (память, ввод-вывод).
 */
void process_markdown_files(const char *input_dir, const char *output_dir);

#ifdef __cplusplus
}
#endif

#endif