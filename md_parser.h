/**
 * @file md_parser.h
 * @brief Парсер Markdown для цепей Маркова с расширенными возможностями.
 *
 * Обрабатывает все .md файлы из входной папки:
 * - извлекает слова с учётом регистра и пунктуации;
 * - распознаёт структуру (заголовки, списки, цитаты);
 * - строит таблицы переходов униграмм (первый порядок) и биграмм (второй порядок);
 * - сохраняет локальные таблицы частот для каждого файла и глобальные таблицы вероятностей.
 */

#ifndef MD_PARSER_H
#define MD_PARSER_H

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Обрабатывает все Markdown-файлы из input_dir и записывает результаты в output_dir.
 *
 * Для каждого файла создаётся .txt с таблицей частот переходов (униграммы).
 * Дополнительно создаются global_unigram.txt и global_bigram.txt с вероятностями.
 *
 * @param input_dir  путь к папке с .md файлами
 * @param output_dir путь для сохранения результатов (создаётся при необходимости)
 */
void process_markdown_files(const char *input_dir, const char *output_dir);

#ifdef __cplusplus
}
#endif

#endif